Analyze the Apache-style access log at `/app/access.log` and write a JSON report to `/app/report.json`.

Success criteria:

1. Create `/app/report.json` containing a valid JSON object.
2. Include a `total_requests` field with the value `6`.
3. Include a `unique_ips` field with the value `3`.
4. Include a `top_path` field with the value `"/index.html"`.
